FROM python:3.12-slim

WORKDIR /app

COPY r3/requirements.txt ./requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

COPY r3/node.py ./node.py

ENV R3_DATA_DIR=/data
EXPOSE 8000

CMD ["sh", "-c", "uvicorn node:app --host 0.0.0.0 --port ${PORT:-8000}"]
